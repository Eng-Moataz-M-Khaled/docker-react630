sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t moatazmkhaled/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true moatazmkhaled/docker-react npm run test